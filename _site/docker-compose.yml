version: '3'
services:
  site:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/site
    environment:
      - JEKYLL_ENV=development
      - FORCE_POLLING=true
    command: bundle exec jekyll serve --host 0.0.0.0 --watch --force_polling 